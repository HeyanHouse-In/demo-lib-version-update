import { demoLibVersionUpdate } from './demo-lib-version-update';

describe('demoLibVersionUpdate', () => {
  it('should work', () => {
    expect(demoLibVersionUpdate()).toEqual('demo-lib-version-update');
  });
});
